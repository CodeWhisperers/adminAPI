{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Map list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Building</th>
                <th>Floor_no</th>
                <th>Img</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_map__show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.building }}</td>
                <td>{{ entity.floorno }}</td>
                <td>{{ entity.img }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_map__show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_map__edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_map__new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
